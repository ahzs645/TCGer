{
  "name": "@tcg/marketing-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:openapi": "node scripts/sync-openapi.mjs",
    "dev": "npm run prepare:openapi && vite",
    "build": "npm run prepare:openapi && vite build",
    "build:local": "npm run prepare:openapi && vite build",
    "preview": "npm run prepare:openapi && vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.6.0",
    "vite": "^7.1.5"
  }
}
