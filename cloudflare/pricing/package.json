{
  "name": "@tcg/hosted-pricing",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test test/*.test.mjs",
    "check": "wrangler deploy --dry-run --outdir .build",
    "import": "node scripts/import-snapshot.mjs"
  },
  "dependencies": {
    "cborg": "^6.1.2",
    "jose": "^6.2.2",
    "node-app-attest": "1.0.1"
  },
  "devDependencies": {
    "miniflare": "5.20260730.0-alpha",
    "wrangler": "^4.118.0"
  }
}
