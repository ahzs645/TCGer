# TCGer storage cleanup — September 14, 2026

Removed **9.53 GB (8.88 GiB)** of allocated generated artifacts. Filesystem free space increased from 19.42 GB to 28.90 GB during the operation; concurrent system activity can affect this measurement. Figures marked GB use decimal units, matching the original 39.7 GB project inventory. The whole project fell from **39.67 GB to 30.14 GB**, and `.artifacts` from **28.39 GB to 23.79 GB**. Exact measurements are recorded in `summary.json`.

| Removed | Allocated size |
|---|---:|
| Generated web, Android and Xcode builds | 3.32 GB |
| Two unused conversion/export environments | 2.47 GB |
| Crop-test variants and archived duplicate folders | 2.66 GB |
| Three clean historical Git worktrees | 0.72 GB |
| Old Hugging Face upload caches | 0.37 GB |

All source photos, frozen dataset releases, saved corner/orientation/stacking decisions, journals, current model checkpoints, active ML/FiftyOne environments, and Node dependencies remain in place. The three new training jobs had completed; their final CPU audit had failed. Its evidence and checkpoints are retained for recovery. This cleanup did not launch training or change the baseline.

Before removal, a fresh durable-state snapshot completed in the configured Drive folder. It covered 2,676 state/code/report files and retained the existing six FiftyOne dataset exports (those databases were stopped). Verification checked 2,677 protected state/code/model files against their original hashes after cleanup, with no changes or missing files. Google Drive remote synchronization was not independently measured.

## Recovery

- Web/Android/Xcode build caches are regenerated by their normal development/build workflows. The first subsequent build will do more work. Source assets and dependency installations were retained.
- Recreate `tmp/arcface-web/venv` or `mobile-apps/ios/scripts/.venv-coreml` with Python 3.11, then install the corresponding saved `arcface-web-requirements.txt` or `coreml-requirements.txt` using that environment's Python. The original `pyvenv.cfg` files record their interpreter setup. The active card-geometry and FiftyOne environments were retained.
- Both crop-parity job archives remain under `.artifacts/card-geometry/crop-parity-2026-09-02/`. Every removed extracted file was checked against its archive: 203 in `job-bundle.I1K5Fd`, 301 in `job-bundle-native.r5nY14`. Extract the appropriate archive into its original directory when needed.
- The `imageEdge-bilinear-inset00-black` reference images, cases, all 67 source photos, native outputs and reports remain. The other 35 reference configurations are generated, not labels. Rebuild them from the repository root with:

```sh
.artifacts/card-geometry/trainer-validation-venv/bin/python tools/card-geometry/crop_parity.py write-reference-grid --cases .artifacts/card-geometry/crop-parity-2026-09-02/cases.json --output .artifacts/card-geometry/crop-parity-2026-09-02/references
```

- `worktrees.json` retains exact commits and restore commands for the three removed historical worktrees. Their only ignored files were Python bytecode or Ruff cache. No branch or commit was deleted.
- Upload caches are regenerated when the old staging directories are uploaded again. Their source files and release metadata remain.

## Evidence and remaining space

The complete deletion plan, per-folder log, protected-file hashes, checks and environment manifests are stored locally in `.artifacts/storage-cleanup-20260914/` and copied into `Reference/TCGer-Labeling/reports/2026-09-14-storage-cleanup/`.

The main remaining footprint is the retained card-geometry datasets and experiment assets. Dataset removal should be a separate archive/restore operation with pinned remote receipts and review-path checks. Do not broadly remove `.artifacts`, release images, or label journals. Keep small protocols/results durable, and remove generated build outputs and temporary unpacked job folders when finished with them.
